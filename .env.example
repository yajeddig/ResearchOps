# ResearchOps - Environment variables template (local runs only).
# In GitHub Actions these are repository secrets. NEVER commit .env.

# === WF1 (Ingest), WF2 (Monitor) and WF4 (Ask) — all Claude ===
ANTHROPIC_API_KEY=your_claude_api_key_here
# CLAUDE_MODEL overrides per process: WF1 defaults to claude-sonnet-5 (high
# volume, per-capture), WF2/WF4 default to claude-opus-5 (low volume,
# monthly report / on-demand Q&A). Set it in the workflow env to override.
CLAUDE_MODEL=                            # optional override
SEMANTIC_SCHOLAR_API_KEY=                # optional, raises the rate limit

# === Notifications (Telegram) ===
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_telegram_chat_id

# === Provided automatically by GitHub Actions ===
# GITHUB_TOKEN, GITHUB_REPOSITORY, GITHUB_OUTPUT, ISSUE_NUMBER, ISSUE_TITLE, ISSUE_BODY
